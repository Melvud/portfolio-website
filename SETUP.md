# 🚀 Быстрый старт - Установка и запуск портфолио

## Шаг 1: Убедитесь, что Node.js установлен

Откройте терминал и выполните:

```bash
node --version
```

Если Node.js не установлен, скачайте его с [nodejs.org](https://nodejs.org/) (рекомендуется LTS версия 18 или выше).

## Шаг 2: Установка зависимостей

Откройте терминал в папке `portfolio-react` и выполните:

```bash
npm install
```

Это установит все необходимые пакеты:
- React 18
- Three.js (для 3D фона)
- Framer Motion (для анимаций)
- Lucide React (иконки)
- Vite (быстрая сборка)

⏱️ Установка займет 1-3 минуты в зависимости от скорости интернета.

## Шаг 3: Запуск в режиме разработки

```bash
npm run dev
```

🎉 Готово! Сайт откроется автоматически по адресу: `http://localhost:3000`

Все изменения в коде будут отображаться моментально (Hot Module Replacement).

## Шаг 4: Сборка для продакшена

Когда будете готовы к деплою:

```bash
npm run build
```

Оптимизированная сборка появится в папке `dist/`.

## 🎨 Кастомизация

### 1. Обновление личной информации

**Файл**: `src/components/Hero/Hero.jsx`

Измените заголовок и описание:

```jsx
<h1>
  Ваш заголовок
  <br />
  <span className="gradient-text">Подзаголовок</span>
</h1>
```

**Файл**: `src/components/Contact/Contact.jsx`

Обновите email и ссылки:

```jsx
href="mailto:ваш@email.com"
href="https://github.com/ваш-username"
href="https://t.me/ваш_telegram"
```

### 2. Добавление реальных скриншотов проектов

**Способ 1**: Замените иконки на изображения

В `src/components/Projects/Projects.jsx` замените:

```jsx
<div className="project-image">
  <div className="project-placeholder">{project.icon}</div>
</div>
```

На:

```jsx
<div className="project-image">
  <img 
    src="/images/your-project-screenshot.png" 
    alt={project.title}
    style={{width: '100%', height: '100%', objectFit: 'cover'}}
  />
</div>
```

**Способ 2**: Положите изображения в папку `public/images/`

```
portfolio-react/
├── public/
│   └── images/
│       ├── project1.png
│       ├── project2.png
│       └── project3.png
```

### 3. Изменение цветовой схемы

**Файл**: `src/styles/index.css`

```css
:root {
  --primary: #00d9ff;     /* Основной цвет (голубой) */
  --secondary: #7c3aed;   /* Вторичный (фиолетовый) */
  --accent: #f59e0b;      /* Акцент (оранжевый) */
  --bg-darker: #050814;   /* Фон */
  --text-primary: #e2e8f0; /* Основной текст */
}
```

### 4. Редактирование проектов

**Файл**: `src/components/Projects/Projects.jsx`

Найдите массив `projects` и отредактируйте:

```jsx
const projects = [
  {
    title: 'Название вашего проекта',
    badges: [
      { text: 'Новое', type: 'featured' },
      { text: 'Live', type: 'live' }
    ],
    icon: '🚀',
    challenge: 'Какую проблему решали',
    solution: 'Как решили',
    result: 'Измеримый результат',
    tech: ['React', 'Node.js', 'PostgreSQL'],
    links: [
      { text: 'Посмотреть', url: 'https://...', icon: 'external' },
      { text: 'GitHub', url: 'https://github.com/...', icon: 'github' }
    ]
  }
  // ... добавьте больше проектов
]
```

### 5. Обновление услуг

**Файл**: `src/components/Services/Services.jsx`

Отредактируйте массив `services`.

## 📱 Деплой (Размещение в интернете)

### Vercel (Рекомендуется - БЕСПЛАТНО)

1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```
3. Выполните:
   ```bash
   vercel
   ```
4. Следуйте инструкциям в терминале

✅ Ваш сайт будет доступен по адресу типа: `your-portfolio.vercel.app`

### Netlify (Альтернатива - БЕСПЛАТНО)

1. Выполните сборку:
   ```bash
   npm run build
   ```
2. Зайдите на [netlify.com](https://netlify.com)
3. Перетащите папку `dist` на страницу Netlify

✅ Сайт будет доступен через несколько секунд!

### GitHub Pages (Для продвинутых)

1. Измените `vite.config.js`:
   ```js
   export default defineConfig({
     base: '/название-репозитория/',
     // ... остальное
   })
   ```

2. Установите `gh-pages`:
   ```bash
   npm install --save-dev gh-pages
   ```

3. Добавьте в `package.json`:
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```

4. Деплой:
   ```bash
   npm run deploy
   ```

## 🐛 Решение проблем

### Ошибка: "command not found: npm"

**Решение**: Установите Node.js с [nodejs.org](https://nodejs.org/)

### Ошибка при установке зависимостей

**Решение**: Попробуйте:
```bash
npm cache clean --force
npm install
```

### Сайт не открывается после `npm run dev`

**Решение**: 
1. Проверьте, не занят ли порт 3000
2. Измените порт в `vite.config.js`:
   ```js
   server: {
     port: 3001
   }
   ```

### Three.js фон не работает

**Решение**: Проверьте консоль браузера (F12) на ошибки. Убедитесь, что Three.js установлен:
```bash
npm install three
```

## 📚 Полезные ресурсы

- [React документация](https://react.dev/)
- [Three.js примеры](https://threejs.org/examples/)
- [Framer Motion](https://www.framer.com/motion/)
- [Vite документация](https://vitejs.dev/)

## 💡 Советы

1. **Используйте git** для контроля версий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **Оптимизируйте изображения** перед добавлением (используйте WebP формат)

3. **Тестируйте на разных устройствах** - сайт адаптивный, но лучше проверить

4. **Следите за размером bundle** - запустите:
   ```bash
   npm run build
   ```
   И проверьте размер файлов в `dist/`

## 🆘 Нужна помощь?

- Откройте issue на GitHub
- Напишите мне: ivsilan2005@gmail.com
- Telegram: @Melvud

---

Удачи с вашим портфолио! 🚀
